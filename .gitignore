# ⚠️ 真实配置含机密（API key / bot token / chat_id），绝不入库；用 config.example.toml 作模板
config.toml

# 含本机绝对路径/用户名的真实 systemd 单元，不入库；用 twitter2telegram.service.example 作模板
twitter2telegram.service

# 运行时数据 / 缓存（含推文内容、会话 id 等，不入库）
state.db
*.db
last_*.json

# Python
__pycache__/
*.pyc
*.pyo

# 日志
*.log
